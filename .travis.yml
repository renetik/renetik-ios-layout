# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

osx_image: xcode10.1
language: objective-c
xcode_workspace: Example/Renetik-iOS.xcworkspace
# cache: cocoapods
podfile: Example/Podfile
#before_install:
#- gem install cocoapods # Since Travis is not always on latest version
#- pod install --project-directory=Example --repo-update
#script:
#- set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Example/CS-IOS.xcworkspace -scheme CS-IOS-Example -sdk iphonesimulator9.3 ONLY_ACTIVE_ARCH=NO | xcpretty
#- pod lib lint
